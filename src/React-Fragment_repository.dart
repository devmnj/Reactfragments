import 'package:new_instagrammundefined/index.dart';

class React-FragmentRepository {
  final React-FragmentProvider _react-FragmentProvider = React-FragmentProvider();

  React-FragmentRepository();

  void test(bool isError) {
    _react-FragmentProvider.test(isError);
  }
}